# ==========================================================
# ENGEB CCNL Simulator / App Consulenza Lavoro (Sprint 0)
#
# Copia questo file in .env e compila i valori.
# Nota: l'app può girare anche senza OAuth (funzioni pubbliche),
# ma le sezioni protette (/my-ccnl, /admin, /users) richiedono auth.
# ==========================================================

# --- Database (OBBLIGATORIO) ---
# Formato: mysql://USER:PASSWORD@HOST:PORT/DB_NAME
DATABASE_URL="mysql://root:password@localhost:3306/engeb_ccnl"

# --- Session/JWT cookie secret (CONSIGLIATO) ---
# Usato per firmare/verificare il cookie di sessione.
# In produzione: usare una stringa lunga e random (>= 32 char).
JWT_SECRET="change_me_to_a_long_random_secret"

# --- OAuth / Auth Server (OPZIONALE) ---
# Se non configurato, l'app resta utilizzabile sulle pagine pubbliche.
# Per abilitare login e funzioni protected/admin, impostare entrambi:
VITE_APP_ID="engeb-ccnl-simulator"
OAUTH_SERVER_URL="https://<your-oauth-server>"

# OpenID owner/admin (OPZIONALE)
OWNER_OPEN_ID=""

# --- AI / Forge (OPZIONALE) ---
# Solo se vuoi abilitare le chiamate LLM (AIChatBox)
BUILT_IN_FORGE_API_URL=""
BUILT_IN_FORGE_API_KEY=""
